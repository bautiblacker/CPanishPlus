/*
** En este ejemplo se puede observar que la variable var1
** resuelve su valor en compilación, mientras que la variable
** var2 lo hará en ejecución.
**
** Para observar esto se puede ejecutar el compilador con el flag -p
** que mantiene una copia del código C intermedio bajo el nombre de
** ".comp_mid.tmp.c".
**
** También se pueden apreciar las precedencias entre operadores.
** En var2 podemos ver el resto y en var1 podemos ver la potencia.
*/

Principal es una funcion que devuelve un entero:
    prueba es una cadena que vale "Prueba".

    Mostrar "Var1:\n".
    var1 es una cadena que vale (3 ^ (13 - 10)) * "Prueba\n".
    Mostrar var1.

    Mostrar "Var2:\n".
    var2 es una cadena que vale (prueba + "\n") * (2 ^ (3 ^ 1)).
    Mostrar var2.

Fin.
